server {
    listen 80;

    root /data;
    rewrite_log on;

    rewrite ^([^.]+[^\/])$ $1/ permanent;

    location ^~ /api/v1/ {
        proxy_pass http://api-gateway:8000;
    }

    location ~ ^\/([^\/]+)\/([^\/]+)\/(.*)$ {
        rewrite ^\/([^\/]+)\/([^\/]+)\/(.*)$ /editor/editor/$3 break;
    }
}
